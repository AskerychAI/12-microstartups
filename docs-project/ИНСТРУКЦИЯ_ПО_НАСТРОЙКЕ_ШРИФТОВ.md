# Инструкция по настройке шрифтов для сайта

Эта инструкция описывает, как изменить шрифт для всего сайта или отдельных элементов.

---

## 📋 Быстрый старт

1. **Откройте файл**: `assets/css/custom.css`
2. **Найдите строку** с `.content { font-family: ... }`
3. **Измените шрифт** на нужный
4. **Сохраните файл**
5. **Опубликуйте изменения** (см. ниже)

---

## 🎨 Варианты настройки шрифта

### Вариант 1: Использование системных шрифтов (без загрузки)

Самый простой способ — использовать шрифты, которые уже установлены на компьютерах пользователей:

```css
/* В файле assets/css/custom.css */
.content {
  font-family: "Inter", "Segoe UI", -apple-system, BlinkMacSystemFont, "Roboto", "Helvetica Neue", Arial, sans-serif;
}
```

**Плюсы**: Быстрая загрузка, нет дополнительных запросов  
**Минусы**: Ограниченный выбор шрифтов

---

### Вариант 2: Google Fonts (рекомендуется)

Используйте красивые шрифты из Google Fonts.

#### Шаг 1: Выберите шрифт

Перейдите на https://fonts.google.com/ и выберите шрифт, например:
- **Inter** — современный, читаемый
- **Roboto** — популярный, универсальный
- **Open Sans** — дружелюбный, читаемый
- **Source Sans Pro** — профессиональный
- **Playfair Display** — декоративный (для заголовков)

#### Шаг 2: Подключите шрифт

Откройте файл `layouts/_partials/custom/head-end.html` и раскомментируйте нужный шрифт:

**Для шрифта Inter:**
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```

**Для шрифта Roboto:**
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
```

#### Шаг 3: Примените шрифт в CSS

Откройте `assets/css/custom.css` и измените:

```css
.content {
  font-family: "Inter", sans-serif; /* или "Roboto", "Open Sans" и т.д. */
}
```

#### Шаг 4: Для всего сайта (опционально)

Если хотите применить шрифт ко всему сайту (включая навигацию, заголовки):

```css
/* В файле assets/css/custom.css */
body {
  font-family: "Inter", sans-serif;
}
```

---

### Вариант 3: Локальный шрифт

Если у вас есть файл шрифта (например, `.woff2`):

#### Шаг 1: Поместите файл шрифта

Создайте папку `static/fonts/` и поместите туда файлы шрифта:
```
static/fonts/
  ├── MyFont-Regular.woff2
  ├── MyFont-Bold.woff2
  └── MyFont-Italic.woff2
```

#### Шаг 2: Добавьте @font-face

В файле `assets/css/custom.css` добавьте:

```css
@font-face {
  font-family: "MyFont";
  src: url("/fonts/MyFont-Regular.woff2") format("woff2");
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "MyFont";
  src: url("/fonts/MyFont-Bold.woff2") format("woff2");
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}
```

#### Шаг 3: Используйте шрифт

```css
.content {
  font-family: "MyFont", sans-serif;
}
```

---

## 📝 Примеры популярных шрифтов

### Inter (современный, минималистичный)

**Подключение** (`layouts/_partials/custom/head-end.html`):
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```

**CSS** (`assets/css/custom.css`):
```css
.content {
  font-family: "Inter", sans-serif;
}
```

### Roboto (Google, универсальный)

**Подключение**:
```html
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
```

**CSS**:
```css
.content {
  font-family: "Roboto", sans-serif;
}
```

### Open Sans (читаемый, дружелюбный)

**Подключение**:
```html
<link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600;700&display=swap" rel="stylesheet">
```

**CSS**:
```css
.content {
  font-family: "Open Sans", sans-serif;
}
```

### Комбинация: Playfair Display для заголовков + Inter для текста

**Подключение**:
```html
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
```

**CSS**:
```css
.content {
  font-family: "Inter", sans-serif;
}

.content h1, .content h2, .content h3 {
  font-family: "Playfair Display", serif;
}
```

---

## 🔧 Дополнительные настройки

### Размер шрифта

```css
.content {
  font-size: 1.1rem; /* Увеличить размер текста (по умолчанию ~1rem) */
}
```

### Межстрочный интервал

```css
.content {
  line-height: 1.8; /* Увеличить расстояние между строками */
}
```

### Настройка заголовков

```css
.content h1 {
  font-weight: 700;
  letter-spacing: -0.02em; /* Уменьшить расстояние между буквами */
}

.content h2 {
  font-weight: 600;
  letter-spacing: -0.01em;
}
```

### Шрифт для кода

```css
@layer theme {
  :root {
    --hx-default-mono-font-family: "JetBrains Mono", "Fira Code", "Consolas", monospace;
  }
}
```

---

## 🚀 Публикация изменений

После изменения шрифтов:

1. **Сохраните файлы**:
   - `assets/css/custom.css`
   - `layouts/_partials/custom/head-end.html` (если использовали Google Fonts)

2. **Проверьте локально** (опционально):
   ```powershell
   cd C:\Hugo\12-microstartups
   hugo server
   ```
   Откройте `http://localhost:1313/` и проверьте шрифт.

3. **Опубликуйте изменения**:
   ```powershell
   git add .
   git commit -m "Настроен кастомный шрифт"
   git push
   ```

4. **Готово!** Через 1-2 минуты изменения появятся на сайте.

---

## ❓ Часто задаваемые вопросы

### Шрифт не применился

1. Проверьте, что файл `assets/css/custom.css` существует
2. Убедитесь, что синтаксис CSS правильный
3. Очистите кеш браузера (Ctrl+F5)
4. Проверьте консоль браузера (F12) на наличие ошибок

### Google Fonts не загружается

1. Проверьте, что вы раскомментировали ссылки в `layouts/_partials/custom/head-end.html`
2. Убедитесь, что ссылка на шрифт правильная (скопируйте с Google Fonts)
3. Проверьте интернет-соединение

### Как вернуть стандартный шрифт?

Закомментируйте или удалите правила в `assets/css/custom.css`:
```css
/* .content {
  font-family: "Inter", sans-serif;
} */
```

### Можно ли использовать разные шрифты для разных элементов?

Да! Например:
```css
.content {
  font-family: "Inter", sans-serif; /* Основной текст */
}

.content h1, .content h2 {
  font-family: "Playfair Display", serif; /* Заголовки */
}

.content code {
  font-family: "JetBrains Mono", monospace; /* Код */
}
```

---

## 🔗 Полезные ссылки

- **Google Fonts**: https://fonts.google.com/
- **Font Pairing (комбинации шрифтов)**: https://www.fontpair.co/
- **Font Squirrel (бесплатные шрифты)**: https://www.fontsquirrel.com/
- **Документация Hextra**: https://imfing.github.io/hextra/

---

**Последнее обновление**: 25 декабря 2025

